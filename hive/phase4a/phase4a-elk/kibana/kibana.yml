server.name: hive-kibana
server.host: "0.0.0.0"

# Elasticsearch connection — uses Docker Compose service name
elasticsearch.hosts: ["http://elasticsearch:9200"]

# Disable telemetry
telemetry.enabled: false
telemetry.optIn: false
